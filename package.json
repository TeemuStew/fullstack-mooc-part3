{
  "name": "fullstack-osa3",
  "version": "0.0.1",
  "description": "osa3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../fullstack-mooc/osa3/puhelinluettelo && npm run build && cp -r dist ../../../fullstack-mooc-part3/",
    "lint": "eslint ."
  },
  "author": "Teemu Mäkelä",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^9.1.1",
    "globals": "^15.1.0",
    "nodemon": "^3.1.0"
  }
}
